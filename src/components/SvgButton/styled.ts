import styled from 'styled-components'

export const Button = styled.div`
  background: none;
  border: none;
  cursor: pointer;

  &:active {
    opacity: 0.5;
  }
`

export const Image = styled.img``
