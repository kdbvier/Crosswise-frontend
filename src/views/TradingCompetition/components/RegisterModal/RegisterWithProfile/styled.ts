import styled from 'styled-components'
import { Checkbox } from '@crosswise/uikit'

export const StyledCheckbox = styled(Checkbox)`
  min-width: 24px;
`

export const StyledLabel = styled.label`
  cursor: pointer;
`
